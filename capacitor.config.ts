import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.herenow.app',
  appName: 'herenow',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
